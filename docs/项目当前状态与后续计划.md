# Satsuma TestLab 项目当前状态与后续计划

> 更新日期：2026-07-26
>
> 代码基线：`master`，包含真实单 VM 验收、SYSTEM 自启动和 RPC 断线修复
>
> 文档状态：已纳入仓库，用于当前阶段交接

## 0. 当前结论

Satsuma 已经完成核心 MVP 的主要代码闭环：Host 能发布任务、VM Agent 能领取并执行任务、执行结果能
落盘并由 Host 汇总；RPC、VMware 生命周期与快照命令、AI 主动检测模式也已经有对应实现和自动化测试。

除本机自动化测试外，当前已经使用一台真实 Windows 11 VMware Guest、NAT/DHCP、SYSTEM Agent、
真实 Shared Folder 和已有基础快照完成单 VM 验收。`check` 的 6 项环境检查与 Guest echo 全部通过，
独立示例软件也完成了 Artifact 部署、执行、日志与文件收集。Agent 已通过关机、启动后的新 PID 和
`boot_id` 证明由 SYSTEM 开机任务自动上线，并通过硬关机、恢复 AI 快照、启动和完整主动检测验收。
双 VM 和危险目标程序仍未验收。

项目目前适合作为早期可运行 MVP 继续开发，不应视为已经达到无人值守、自动恢复或生产级安全标准。

| 维度 | 当前状态 |
|---|---|
| 核心文件任务闭环 | 已实现并通过自动化测试 |
| RPC 注册、心跳和状态上报 | 已实现并通过本机回环测试 |
| VMware 命令封装 | 已实现，自动化测试使用 Fake vmrun |
| 主动检测模式 | 已实现并通过 Host/Agent 并发集成测试 |
| 真实 VMware 环境 | 单台 NAT VM 和 AI 快照恢复已验收；双 VM 尚未验收 |
| Guest 无人值守启动 | 已实现一次安装、SYSTEM 开机任务，并通过真实 VM 关机重启验收 |
| 自动恢复和完整任务状态机 | 单 VM 编排、原子状态、证据归档和 claim 租约已实现；崩溃续跑待完成 |
| 生产级认证、安装和运维 | 尚未实现 |

## 1. 当前整体的代码结构思路

### 1.1 项目定位和责任边界

Satsuma 是 Host 与隔离 Windows VM 之间的测试执行基础设施。它负责：

- 控制 VMware VM 的启动、停止和快照恢复。
- 将 Host 构建产物物化为带哈希的任务 Artifact。
- 让 VM Agent 在 Guest 本地目录执行程序。
- 管理进程树、超时、stdout、stderr 和结果文件。
- 保存机器可读执行证据，并向上层 AI 返回平台状态。

Satsuma 不判断被测程序的业务逻辑是否正确。业务断言、测试参数、结果解释和目标项目修改由调用它的
AI 或程序员负责。

### 1.2 双端程序和三条通道

项目构建两个运行程序，共享一个静态核心库：

```text
SatsumaCore（静态库）
  ├── SatsumaHost.exe（宿主机）
  └── SatsumaVM.exe（每台测试 VM）
```

Host 与 VM 之间使用三条互补通道：

```text
调用方 AI / 程序员
        |
        v
SatsumaHost.exe
  ├── VMware Shared Folder：任务、Artifact、状态、日志和结果
  ├── coro_rpc：Agent 注册、心跳、任务通知和实时 Job 状态
  └── vmrun.exe：VM 电源、快照和 Guest 网络之外的带外控制
        |
        v
SatsumaVM.exe
  ├── 校验并领取任务
  ├── 复制 Artifact 到 Guest 本地工作目录
  ├── 使用 Windows Job Object 执行和超时终止进程树
  └── 原子发布 execution.json、日志和收集文件
```

共享目录是持久事实来源；RPC 用于低延迟通知和状态；`vmrun` 用于 VM 生命周期和最终带外恢复。
即使 RPC 暂时不可用，Agent 的 `--watch` 模式仍会扫描共享目录任务。

### 1.3 源码目录职责

```text
satsuma-testlab/
├── include/satsuma/core/       公共配置、任务、路径、哈希和协议模型
├── include/satsuma/rpc/        Host/Agent RPC 服务契约
├── src/core/                   SatsumaCore 的实现
├── src/host/                   Host Controller、诊断、RPC Server 和 CLI
├── src/vm/                     VM Agent、进程执行器、RPC Client 和 CLI
├── src/vmware/                 vmrun Provider
├── schemas/                    lab、agent、task 和 result JSON Schema
├── examples/                   Agent 配置和无害任务示例
├── tests/                      单元、回环、Fake vmrun 和 Host/VM 集成测试
├── docs/SatsumaReadme.md       面向用户的 VMware 与配置指南
├── docs/SatsumaSkill.md        面向 AI 的环境发现和使用契约
├── lab.json                    Host 实验室配置模板
├── CMakeLists.txt              两个程序、静态库和测试目标
└── CMakePresets.json           Windows Debug/Release 构建预设
```

核心组件的职责如下：

| 组件 | 当前职责 |
|---|---|
| `src/core/config.cpp` | 读取并校验 `lab.json` 和 `agent.json` 的关键字段 |
| `src/core/task.cpp` | 解析任务、生成运行清单、序列化执行结果 |
| `src/core/path.cpp` | 限制相对路径，防止越界和重解析点逃逸 |
| `src/core/json_io.cpp` | JSON 读取和临时文件原子发布 |
| `src/core/sha256.cpp` | Artifact 和结果文件 SHA-256 |
| `src/host/controller.cpp` | 物化运行目录、复制 Artifact、生成报告 |
| `src/host/diagnostics.cpp` | 环境检查和 Agent 主动 echo 探针 |
| `src/host/rpc_service.cpp` | Agent 会话、心跳、任务选择和 Job 状态 |
| `src/vm/agent.cpp` | 扫描、claim、执行、收集和结果发布 |
| `src/vm/autostart.cpp` | 原生注册、更新并启动 SYSTEM 开机计划任务 |
| `src/vm/process_runner.cpp` | Win32 进程创建、参数传递、Job Object 和超时 |
| `src/vmware/vmrun_provider.cpp` | 结构化调用 `vmrun.exe` 并处理超时和错误 |

### 1.4 当前运行目录模型

Host 为每次任务创建唯一 `run_id`，先在点号前缀暂存目录中完成写入，再整体发布为 Agent 可见目录：

```text
shared_folder.host_root/runs/<run_id>/
├── task.json
├── artifacts/
│   └── <vm-id>/...
├── state/
│   ├── <vm-id>/<step-id>.claim.json
│   └── <vm-id>-agent.json
└── results/
    └── <vm-id>/<step-id>/
        ├── execution.json
        ├── stdout.log
        ├── stderr.log
        └── files/...
```

`claim.json` 通过独占创建避免同一步骤被多个 Agent 会话重复领取。Artifact 先在共享目录校验哈希，
再复制到 `agent.json` 的 `local_work_root/<run_id>/` 执行，不直接从 UNC 路径运行程序。

### 1.5 当前命令入口

Host 已实现：

```text
SatsumaHost check
SatsumaHost serve
SatsumaHost vm start|stop|restore
SatsumaHost snapshot list|create-ai|delete-ai
SatsumaHost run
SatsumaHost orchestrate
SatsumaHost report
```

VM Agent 已实现：

```text
SatsumaVM --once
SatsumaVM --rpc-once
SatsumaVM --validate-config
SatsumaVM --watch
SatsumaVM --install-autostart
```

任务步骤当前支持 `echo` 和前台 `execute`；Host 生命周期策略支持单 VM 的 `restore_before`、
成功/失败动作和 `finally`。

## 2. 已经做完的部分

### 2.1 工程基础和仓库规则

- 已建立 Windows/C++20/CMake 工程。
- 已生成 `SatsumaCore`、`SatsumaHost` 和 `SatsumaVM` 三个构建目标。
- 已固定 `nlohmann/json v3.12.0` 和 `yalantinglibs 0.6.1` 的获取策略。
- 已配置 MSVC `/W4`、`/permissive-`、`/utf-8` 和 C++ 异常选项。
- 已配置 `.editorconfig`、`.gitattributes`、VS Code 保存规则和仓库级 `core.autocrlf=false`。
- 已提供 Debug/Release CMake Preset。

### 2.2 配置、协议和安全基础

- 已定义 `lab.json`、`agent.json`、任务和执行结果的数据模型及 schema。
- 已校验 schema/protocol 版本、稳定 ID、VM 唯一 ID、端点和轮询/超时范围。
- 已实现 Windows UTF-8 路径转换和结构化命令行参数编码。
- 已实现相对路径验证、根目录约束和重解析点防护。
- 已实现 SHA-256 文件校验。
- 已实现 JSON 临时文件完整写入后原子改名。
- 已拒绝未登记 Artifact、绝对任务路径和 `..` 越界路径。

### 2.3 Host 文件任务闭环

- `run` 能读取任务文件、校验 VM 引用并生成唯一或调用方指定的 `run_id`。
- Host 能复制 Artifact、计算实际 SHA-256，并验证可选的预填哈希。
- Artifact 目标被限制在 `artifacts/` 子目录。
- 运行目录在隐藏暂存阶段准备完成后一次性发布，Agent 不会读取半成品 manifest。
- `report` 能统计预期、已报告、成功和失败步骤，并返回执行明细。
- Controller 同时支持从 JSON 文件和内存任务计划物化运行，供主动检测复用。

### 2.4 VM Agent 执行闭环

- Agent 能扫描共享目录并只处理匹配 `lab_id`、protocol 和 `vm_id` 的步骤。
- Agent 能独占创建 claim，避免正常并发场景下的重复领取。
- Agent 能校验 Artifact 哈希，复制到 Guest 本地目录后执行。
- `echo` 步骤能生成无害 stdout 结果。
- `execute` 步骤支持参数数组、本地工作目录和有限超时。
- Windows Job Object 能在超时时终止被测进程树。
- stdout/stderr 使用 `.partial` 文件写入，完成后原子发布最终日志。
- Agent 能收集任务声明的结果文件并记录 SHA-256。
- `execution.json` 记录状态、退出码、超时、耗时、日志路径、错误和收集文件。
- RPC 暂时不可用时，磁盘任务和结果通道仍可工作。

### 2.5 RPC 控制通道

- 已定义版本化的 Agent 注册、心跳、任务轮询和 Job 状态 DTO。
- Host 能校验 `lab_id`、`vm_id`、Agent 版本、`session_id` 和 `boot_id`。
- Agent 能注册、心跳、接收任务引用并上报运行/完成状态。
- Agent 能在 RPC 断开后重连，并继续扫描共享目录。
- Host `serve` 能在 `lab.json` 指定的地址启动 `coro_rpc` Server。
- 已有本机回环测试覆盖 RPC 注册、状态、任务发现和结果上报。

### 2.6 VMware 带外控制和快照保护

- `vmrun list` 运行状态查询。
- VM 无界面启动。
- VM 软关闭和硬关闭。
- 恢复指定快照。
- 查询快照列表、创建快照和删除快照。
- `vmrun` 通过 `CreateProcessW` 接收结构化参数，不使用拼接的 `cmd /c`。
- `vmrun` 进程受超时和 Job Object 约束。
- 用户基础快照与 AI 派生快照有不同所有权。
- AI 快照使用配置前缀、时间戳和数量配额。
- Host 拒绝删除用户基础快照或非 AI 所有权快照。
- AI 快照创建/删除状态会写入 `archive_root/snapshots/<vm-id>/` 元数据。
- 已提供默认关闭的真实 VMware 生命周期 smoke 目标，要求显式确认字符串后才允许运行。

### 2.7 Guest 无人值守启动

- `SatsumaVM --watch` 每次启动都会创建或覆盖 `\Satsuma\SatsumaVM Agent` 任务定义。
- 计划任务使用 `SYSTEM`、最高权限、开机延迟、无限运行时限、单实例和失败重启策略。
- `SatsumaVM --install-autostart` 完成注册后立即启动后台任务，供安装脚本调用。
- `SatsumaVM --validate-config` 只做配置解析预检，不会创建任务或访问 VMware。
- `--once` 和 `--rpc-once` 保持无系统副作用，不会在开发机或测试进程中创建任务。
- `install-agent.ps1` 能自动请求 UAC、保护本地安装 ACL、暂存并校验文件、停止旧任务和旧进程、失败
  回滚、更新文件并启动后台 Agent。
- `--watch` 会同时容忍 RPC 和 Shared Folder 暂时不可用，避免 VMware Tools 启动竞态导致进程退出。
- Agent 会在共享目录发布带 PID 的 `agents/<vm-id>.json` 就绪状态，供安装脚本和诊断核对实际进程。

### 2.8 AI 主动检测模式

`SatsumaHost check` 已实现以下检查：

- Host 共享目录是否存在并支持原子写入。
- Host 归档目录是否存在并支持原子写入。
- `vmrun.exe` 是否存在。
- `vmrun list` 是否能执行。
- 目标 VMX 是否存在。
- 快照列表是否包含配置的用户基础快照。
- 自动发布唯一 echo 任务并等待目标 Agent 返回。
- 校验 Agent 结果状态、退出码、超时标记和 stdout 内容。

报告区分：

- `ready` / 退出码 0：环境与 Agent 探针均正常。
- `degraded` / 退出码 3：Agent 探针正常，但某项 VMware/Host 环境检查失败。
- `failed` / 退出码 1：Agent 超时、结果失败或内容不匹配。

### 2.9 配套文档

- 根目录 `README.md` 已提供用户和 AI 文档入口。
- [SatsumaReadme.md](SatsumaReadme.md) 已说明 VMware 网络、Shared Folder、`lab.json`、
  `agent.json`、管理员启动、基础快照和首次验收。
- [SatsumaSkill.md](SatsumaSkill.md) 已约定 AI 的只读环境发现、用户配置清单、主动检测门禁、
  任务生成、取证和恢复边界。
- 已提供 `lab.json`、`examples/agent-client.json` 和 `examples/hello-vm-task.json` 模板。

### 2.10 当前自动化验证结果

2026-07-26 在本机现有构建目录重新执行：

| 配置 | 结果 |
|---|---|
| Debug | 7/7 通过 |
| Release | 7/7 通过 |

当前 7 项测试为：

1. `SatsumaCoreTests`
2. `SatsumaVmAutostartTests`
3. `SatsumaDemoAppTest`
4. `SatsumaHostRpcServiceTests`
5. `SatsumaVmrunProviderTests`
6. `SatsumaRpcServerTests`
7. `SatsumaHostVmFlow`

其中 `SatsumaHostVmFlow` 使用本机目录模拟共享文件夹，并发运行 Host 主动检测与 Agent 驱动；
`SatsumaVmrunProviderTests` 和集成流程使用 `SatsumaFakeVmrun`，不等价于真实 VMware 验收。

同日真实单 VM 验收结果：

- `SatsumaHost check` 返回退出码 0、顶层状态 `ready`，6/6 环境检查通过。
- Guest echo 成功，退出码 0，无超时。
- 独立 `SatsumaDemoApp.exe` 任务 1/1 步骤成功，收集的 JSON 和转换文本内容、SHA-256 均正确。
- Host RPC 正常停止时 Agent 能完成本机重连；真实强杀随后暴露出第三方 `call_for` 卡死与跨线程访问冲突，
  修复和待验收候选见本节后续记录。
- Agent 安装到 `C:\Satsuma` 并注册为 SYSTEM 开机任务；关机后重新启动时 PID 从 8168 变为 7884，
  `boot_id` 从 `boot-20260726080511-88e3de7ef786f462` 变为
  `boot-20260726080759-8e2ee3e9a1c52921`，随后完整 `check` 再次返回 `ready`。
- 已创建可供恢复验收使用的 AI 快照 `satsuma-ai-agent-autostart-ready-20260726080714`；用户基础快照
  `new clear 19045.6456` 保持只读，未被修改。
- 连续 20 次真实 DemoApp 任务全部成功，无丢失、重复 `run_id` 或超时；单步耗时 38–49 ms，平均
  41.35 ms，汇总保存于 Host 归档 `validation/stage0-repeat-20260726083302.json`。
- 已真实执行硬关机、恢复上述 AI 快照、启动和 Agent 自动上线；PID 从 7884 变为 8312，`boot_id`
  变为 `boot-20260726083400-43fa805c6e9ed50a`，最终 `check-20260726083418-6611c68a672912f7`
  返回 `ready`，恢复证据保存于 Host 归档 `validation/stage0-recovery-20260726083420.json`。
- 已使用 `orchestrate` 在真实 VM 完成 `restore_before`、启动、Agent 门禁、DemoApp、`finally` 和证据归档；
  `lifecycle-real-20260726090044` 以 9 次原子迁移进入 `completed`，Agent 以 PID 2620 和新 `boot_id`
  `boot-20260726090147-96606d32ed961f0c` 上线。
- claim v2、人工门禁和 `report --wait-seconds` 已通过 Debug/Release 7/7；最终恢复后的真实门禁
  `check-20260726092829-97cbb22796bf917c` 返回 `ready`，Agent PID 为 6744，`boot_id` 为
  `boot-20260726092817-1e6adad6f7e35066`。
- Guest 已安装 `SatsumaVM-claim-v2-202607260930.exe`，PID 8940 和新 `boot_id`
  `boot-20260726093207-3003af3c47cd671a` 证明切换成功；真实 `check` 返回 `ready`，claim schema v2、
  attempt 和有限租约均已落盘。
- 替换 Host Server 后，该 Agent 的 presence 停在 `2026-07-26T09:34:44.509Z`，进程未退出且任务未重启。
  根因是 `RpcClient::disconnect()` 对已失效 executor 执行无超时同步 drain。`2ee81ae` 已改为有限等待的
  独立清理线程，并增加 Server 停止、同端口重启和 Client 重连测试。
- Guest 安装 `SatsumaVM-rpc-reconnect-202607260940.exe` 后，claim v2 与诊断 `retry_safe=true` 均通过；
  但真实强杀 Server 时 presence 只再更新一次便停止，证明阻塞发生在第三方 `call_for` 内部，而不是随后
  的 `disconnect()` drain。
- Guest 安装 `SatsumaVM-rpc-hard-timeout-202607261015.exe` 时，计划任务进程未产生 presence 并触发安装
  回滚；同一二进制在 Guest 前台执行 `--rpc-once` 返回 `0xC0000005`。该结果证明 `e6b315e` 的每次调用
  临时线程破坏了第三方 Client 的线程约束，不能作为外层硬超时实现。
- `ca819ca` 已恢复同线程同步 `call_for`，并把 watch 模式拆分为文件主循环、RPC 专用线程和独立 watchdog；
  文件线程只把 Job 状态放入有限队列，RPC 永久卡死时进程以 `ERROR_TIMEOUT` 退出并由计划任务重启。
  Debug/Release 均通过 7/7，本机 Release `--rpc-once` 返回 0。新候选
  `SatsumaVM-rpc-thread-isolation-202607261851.exe` 的 SHA-256 为
  `327F40335CAB5D1DF6AC7F0428226237EEB19739FA038E9D0D0B4FE1D0937AB6`，对应安装脚本为
  `install-agent-rpc-thread-isolation-202607261851.ps1`，等待 Guest 安装和真实强杀验收。

## 3. 还没有做完的部分

### 3.1 真实实验室仍缺少的验收

- 当前仅验收一台 NAT/DHCP Windows VM，尚未完成 Client/Gateway 双 VM 编排。
- 尚未验证危险测试专用的 Host-only/LAN Segment 隔离网络。
- 已真实执行软关机、硬关机、恢复 AI 快照、重新启动和 Agent 自动上线。
- 已接入独立于 Satsuma 的 `SatsumaDemoApp.exe` 验收 Artifact、参数、日志和结果收集；尚未接入用户实际
  业务项目和业务断言。
- 已完成连续 20 次真实任务测试；更长时间稳定性和共享目录瞬断仍待后续故障注入覆盖。

### 3.2 JSON 任务生命周期不完整

单 VM 的 `restore_before`、启动、Agent Ready、部署、执行、收集、`finally`、成功/失败清理和
`RECOVERY_FAILED` 已接入 `orchestrate`。仍未实现：

- Host 崩溃后根据持久化阶段自动续跑或转入正式人工确认状态。
- 跨步骤的重试、退避和幂等策略。
- 多 VM 操作的独立状态、部分恢复和统一 teardown。

### 3.3 多 VM 编排能力有限

- 多台 Agent 可以各自领取属于自己的步骤，但 Host 尚无显式依赖图和跨 VM 屏障。
- 尚不支持“Gateway 先启动并 Ready，再执行 Client”的依赖语义。
- 尚不支持后台步骤、长期服务、显式 stop、wait-ready 或健康检查步骤。
- 尚不支持并行度、失败传播和多 VM 统一 teardown 策略。

### 3.4 崩溃恢复和 claim 租约仍需完善

claim v2 已记录有限租约、attempt、`session_id`、`boot_id` 和 `retry_safe`。过期后只有启动身份变化且
显式安全的步骤会原子归档旧 claim 并重试；其他步骤生成 Host 可见的人工门禁。尚缺：

- Host 对失联 Agent 的任务回收决策。
- Host 自身崩溃后根据持久化阶段、VM 状态和 Agent 身份决定续跑或保持门禁。
- 长时间步骤的主动续租；当前租约按步骤超时加固定缓冲一次性计算。

### 3.5 运行归档和报告仍是最小版本

- `orchestrate` 已自动归档主任务和 `finally` 的不可覆盖证据；普通 `run` 尚未自动归档。
- `report` 支持可选的 `--wait-seconds` 有限等待；尚未形成跨命令统一超时参数。
- 尚未生成完整 Host 操作日志、vmrun 日志和恢复报告。
- 尚未实现归档保留期、空间配额、清理策略和证据只读保护。
- 尚未实现面向多个运行的查询、索引或历史摘要。

### 3.6 RPC 和部署安全仍是开发级

- RPC 当前没有 TLS、身份凭据或消息签名，CMake 中明确关闭了 SSL 集成。
- 普通可信测试已验证 NAT 网络；危险测试仍应使用 Host-only/LAN Segment 和防火墙作用域隔离。
- 尚未实现密钥轮换、Host 身份固定或 Guest 凭据存储。
- 尚未形成安装包、签名二进制、升级流程或回滚流程。
- Agent 目前通过原生 SYSTEM 计划任务运行，不是 Windows Service；正式服务安装包仍未实现。
- 尚未实现配置版本迁移和 Host/Agent 升级兼容矩阵。

### 3.7 CLI 和诊断仍有缺口

- 尚未实现设计稿中的细粒度 `file push`、`exec`、`status`、`wait` 等交互命令。
- `check` 不会自动启动 VM 或 Agent。
- `check` 尚未单独报告 VMware Tools 版本、Guest Tools 响应和 Agent 二进制哈希。
- `management_ip` 是可选元数据，NAT/DHCP 环境可以省略；主动检测不依赖该字段。
- JSON Schema 已提供，但运行时主要使用 C++ 手写校验，需要持续保证两者一致。
- 错误码和错误 JSON 尚未形成覆盖所有 CLI 失败场景的统一协议。

### 3.8 测试覆盖仍需扩展

- 真实 VMware 测试尚未执行。
- 尚无真实双 VM 时序、断网、Guest 卡死、Tools 失效和恢复快照测试。
- 尚无 Agent claim 后崩溃、Host 中途重启、磁盘满和共享目录瞬断测试。
- 尚无 RPC 未授权连接、恶意/损坏 manifest 和版本升级兼容测试矩阵。
- 尚无长时间稳定性、重复 20 次以上真实任务和大 Artifact 压力测试。
- 当前只支持 Windows/MSVC，尚无其他平台目标。

## 4. 需要程序员或用户手动配合的部分

完整步骤见 [SatsumaReadme.md](SatsumaReadme.md)。以下事项无法在当前无 VMware GUI、无 Guest 控制
和无管理员权限的命令行环境中可靠完成。

### 4.1 Host 上的人工准备

1. 安装并许可 VMware Workstation，确认真实 `vmrun.exe` 路径。
2. 确定仓库、共享目录、归档目录和 VMX 的真实绝对路径。
3. 确定 Guest 可达的 VMware NAT 或 Host-only Host 地址。
4. 如使用 RPC，使用管理员权限把 TCP 37100 仅放行到对应 VMware 网段。
5. 检查磁盘空间，确保共享目录和归档目录不包含源码、用户目录或凭据。
6. 将确认后的真实值写入已忽略的 `lab.local.json`。

### 4.2 VMware GUI 中的人工准备

1. 创建或选择任意可丢弃的 Windows 测试 VM，不要求特定名称。
2. 安装 VMware Tools。
3. 普通测试可保留 NAT/DHCP；危险网络测试再添加隔离实验网卡。
4. 为每台 VM 添加相同的专用 Shared Folder，并选择始终启用。
5. 确认每台 VM 的 `.vmx` 文件绝对路径。
6. 建议从干净基础 VM 克隆出 Client、Gateway 等实例。

### 4.3 每台 Guest VM 内的人工准备

1. 为当前 VM 创建独立 `agent.json`，设置唯一逻辑 `vm_id`。
2. 验证 Guest 能读写 `\\vmware-host\Shared Folders\<share-name>`。
3. 执行一次 `scripts/install-agent.ps1` 并确认 UAC；脚本完成安装、更新、开机任务和后台启动。
4. 清理遗留测试进程、路由、DNS、适配器和临时文件。

### 4.4 用户基础快照

1. 确认 VMware Tools、管理网络、Shared Folder 和 Agent 均已验证。
2. 确认没有运行中的 Satsuma Job 或被测程序。
3. 推荐关闭 VM 后创建 `clean` 基础快照。
4. 将快照名写入 `lab.local.json` 的 `vms[].snapshots.base`。
5. 基础快照只能由用户维护，AI 不应覆盖或删除。

### 4.5 真实验收需要一次性授权

建议按以下顺序进行：

1. 将目标 VM 声明为可丢弃测试机，并授权 Satsuma 对其执行启停和快照恢复。
2. 启动 VM，等待计划任务自动启动 Agent。
3. 对单台 VM 执行 `SatsumaHost check --vm <id>`。
4. 对全部 VM 执行不带 `--vm` 的主动检测。
5. 连续运行无害 echo 任务，确认没有丢失、重复和超时。
6. 运行一个可丢弃的真实测试 Artifact，检查日志、退出码和收集文件。
7. 显式启用并运行 `SatsumaRealVmwareSmoke`；已有实验室授权范围内不再逐次询问用户。

### 4.6 目标项目接入时需要程序员提供

- 明确的测试程序绝对路径和构建方式。
- 每个程序的参数、预期退出码和最大运行时间。
- 应收集的日志、JSON、抓包或其他结果文件相对路径。
- 业务通过/失败的判断规则。
- 测试是否会安装驱动、修改路由/DNS/防火墙或要求重启。
- 测试前后需要的快照和人工安全边界。

## 5. 未来要做的部分

### 阶段 0：建立真实实验室并关闭当前最大风险

目标：把“模拟环境可用”升级为“真实 VMware 最小闭环可用”。

计划：

1. 已完成单 VM 的 VMware、NAT 网络、共享目录、SYSTEM Agent 和快照准备。
2. 已使用被 Git 忽略的 `lab.local.json` 和 Guest 本地 `agent.json` 完成真实配置。
3. 已完成单 VM 主动检测；双 VM 检测留待阶段 2 环境准备。
4. 已连续执行 20 次真实 DemoApp 任务，无重复、丢失、超时或失败。
5. 已执行硬关机、恢复 AI 快照、启动和 Agent 重新上线验收。
6. 已接入不属于 Satsuma 核心库的 `SatsumaDemoApp.exe` 并完成真实执行取证。

验收：单 VM 的 `check` 返回 `ready`；20 次任务证据完整；VM 能从受保护的 AI 快照恢复并重新上线。
阶段 0 的单 VM 目标已完成，双 VM 环境与编排统一留到阶段 2。

### 阶段 1：实现完整运行状态机和自动恢复

目标：复杂测试失败后也能有确定的停止、收集和恢复结果。

计划：

1. 已引入准备、恢复、启动、等待 Agent、部署、执行、收集、`finally`、恢复和终态模型。
2. 已为任务增加 `restore_before`、成功/失败动作和 `finally` schema。
3. 已实现执行前恢复、失败后恢复和独立的 `RECOVERY_FAILED`。
4. 已在每次状态迁移时原子保存 Host 运行记录。
5. 已增加 claim 租约、过期安全回收和人工确认门禁；Host 崩溃续跑判断仍待完成。
6. 已自动把编排任务的最终证据复制到 Guest 不可见的归档目录。

验收：在 Agent 崩溃、步骤超时和 RPC 断线三类故障下，Host 都能保留证据并报告真实恢复结果。

### 阶段 2：增强任务编排和交互式调试

目标：支持真实双 VM 服务/客户端实验和 AI 逐步诊断。

计划：

1. 增加步骤依赖、跨 VM 屏障和并行度。
2. 增加后台进程、wait-ready、stop-job 和健康检查步骤。
3. 增加 `status`、`wait`、受控 `file push` 和单步 `exec` CLI。
4. 保证交互命令同样物化为运行清单和证据，不产生旁路状态。
5. 为长期服务设计安全停止和完整进程树清理。

验收：Gateway 服务 Ready 后 Client 才启动；任一步失败都会停止相关进程并进入统一清理流程。

### 阶段 3：提高安全性、诊断和可观察性

目标：从可信本机原型提升为可长期维护的隔离实验工具。

计划：

1. 为 RPC 增加认证和可选加密，限制管理网来源。
2. 增加 Agent 二进制/版本指纹和配置一致性检查。
3. 检查 VMware Tools 状态、Guest 响应、管理 IP 和共享目录健康度。
4. 统一 CLI 错误 JSON、退出码和诊断建议。
5. 增加 Host、Agent、vmrun 和恢复过程的结构化日志。
6. 增加归档索引、磁盘配额、保留期和清理策略。

验收：未授权 Agent 被拒绝；常见环境故障可以由机器可读报告准确分类，不依赖人工猜测。

### 阶段 4：产品化和运维

目标：让用户能够稳定安装、升级和恢复环境。

计划：

1. 生成签名的 Release 包和版本清单。
2. 提供 Host/Agent 安装、升级、卸载和回滚流程。
3. 评估是否需要把现有 SYSTEM 计划任务升级为 Windows Service。
4. 增加 `doctor`/配置校验工具和 schema 迁移。
5. 建立 CI、真实 VMware 专用测试机和定期稳定性测试。
6. 形成 Agent 版本与基础快照版本的兼容矩阵。

验收：新测试机可按文档完成安装，升级失败可回滚，真实环境回归有稳定记录。

### 阶段 5：接入真实复杂项目

目标：证明 Satsuma 能作为通用 AI 测试基础设施使用。

计划：

1. 接入至少一个需要隔离网络或管理员权限的外部项目。
2. 由目标项目 AI 生成 Artifact 和任务，不修改 Satsuma 核心业务逻辑。
3. 收集退出码、UTF-8 日志、JSON 结果和必要的系统状态。
4. 在双 VM、断网、超时和失败恢复场景下重复运行。
5. 根据真实使用反馈收敛任务 schema 和 CLI，而不是提前加入未验证抽象。

验收：外部项目能够重复完成“部署、执行、取证、恢复”，并且平台失败与业务失败可以清楚区分。

## 6. 建议的下一步

当前优先级最高的是完成阶段 1 的失联判断、Host 崩溃续跑门禁和故障注入；双 VM 环境
就绪后再扩展依赖、屏障和后台服务编排。

在真实环境准备之前，代码侧可以并行推进但不依赖 VMware 的部分包括：

1. Host 崩溃后按生命周期状态决定续跑或人工门禁。
2. Agent 中断与 RPC 断线的编排故障注入。
3. claim 主动续租和 Host 失联判断。
4. 统一错误 JSON 和跨命令超时语义。
5. RPC 认证威胁模型和配置设计。

用户需要一次性确认哪些 VM 可被丢弃和恢复。确认后，AI 可以在配置范围内自主启停和恢复，不应逐次
打断用户；基础快照删除、未知 VM、防火墙和网络拓扑修改仍需要新的明确授权。
