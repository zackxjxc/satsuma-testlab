# 安全策略

## 支持版本

Satsuma TestLab 当前为 1.0 之前的版本。安全修复只应用于最新发布版本，不维护更早的快照版本。

## 报告漏洞

疑似安全漏洞不应通过公开 Issue 报告。请使用仓库的 GitHub Security Advisory 私密报告功能，并提供：

- 受影响的版本或提交；
- Host 和 Guest 的 Windows 版本；
- 复现所需的信任级别和权限；
- 最小复现步骤；
- 预期影响和已知缓解措施。

如果无法使用私密 Security Advisory，请先通过私密渠道联系仓库所有者，再披露利用细节。维护者应在收到
完整报告后的七天内确认，并在修复或形成明确缓解措施后协调披露。

## 安全边界

Satsuma 假设运行于可信实验室。Host 配置、任务计划、Artifact、VMCI 网关访问者和 Guest 管理员属于
同一信任域。Agent 以 LocalSystem 运行，用于执行可信测试软件；它不是恶意软件沙箱，也不提供多租户隔离。

项目没有声明 Artifact 沙箱，因此逃逸此类假设沙箱不在受理范围内。路径穿越、跨运行结果伪造、Claim
fencing 绕过、意外 Host 执行、控制 `lab.json` 范围外的 VM，以及更新身份或完整性失效属于受理范围。
