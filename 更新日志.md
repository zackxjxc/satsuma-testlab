# 更新日志

本文件记录项目的重要变化。首个稳定版本发布后，项目将遵循语义化版本规则。

## 未发布

### 新增

- Host 运行列表、文件取消和已完成运行保留命令。
- 按运行隔离的 Agent 故障处理和诊断错误文件。
- Artifact、参数、收集文件和进程输出容量限制。
- 取消、Presence、Claim、运行清单、生命周期和报告的 JSON Schema。
- Windows Debug/Release CI、CMake 安装规则和 ZIP 打包。
- 公开的架构、用户、协议、开发、安全和贡献文档。

### 变更

- Shared Folder 成为唯一生产任务和状态通道。
- Host 与 VM 生产代码拆分为可供测试复用的库和轻量可执行程序入口。
- 配置解析器会拒绝未知字段和已移除字段，不再静默忽略。
- 报告命令使用不同退出码区分业务失败、超时和人工介入。
- Host 归档成功后通过显式请求清理 Guest 本地工作目录，并等待 Agent 回执。

### 删除

- 未使用的网络控制实现及其依赖。
- 旧计划任务启动实现及其命令行入口。
- 与当前产品冲突的过期设计方案和内部验收记录。

## 0.2.0 - 2026-07-29

### 新增

- Guest inventory 首次采集、摘要引用、自愈补发，以及 Host 查询和显式刷新。
- CMD、Windows PowerShell 5.1 和 PowerShell 7 的受控脚本 Artifact 执行。
- 实验室进程互斥、持久租约、状态查询、恢复、强制解锁和普通运行 finalize。
- 统一 Guest 存储根、交互用户按运行 ACL、清理策略和 Agent 清理确认。
- spdlog、stdout/stderr、精确 `collect_files` 和崩溃收集边界文档。

### 变更

- 运行清单协议升级到 v3，用户任务 schema 2 支持 `script` 和 `cleanup`。
- Agent 安装优先使用合适的 `D:\SatsumaTestLab`，否则回退到 `%ProgramData%\SatsumaTestLab`。
- `check`、`run` 和 `orchestrate` 在业务任务前验证 inventory 与内部 diagnostic echo。
- Host 归档逐文件校验后才允许删除共享运行；清理失败保留证据和实验室租约。

## 0.1.0 - 未发布

面向 Windows 和 VMware Workstation 的首个公开预览版本。
